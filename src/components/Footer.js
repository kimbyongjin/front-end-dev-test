import React from 'react';

const Footer = (props) => {

  return (
    // TODO: Build component for final footer with 'follow us' and insta + fb svg images
    <div className="footer-container">Footer Container</div>
  );
};

export default Footer;
